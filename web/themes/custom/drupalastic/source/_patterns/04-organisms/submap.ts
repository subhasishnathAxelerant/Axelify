(function () {
  const layout__container__front: any = document.getElementsByClassName(
    'layout-container-front',
  )[0];
  if (layout__container__front) {
    // only run in home page
    // const subform = document.getElementsByClassName('webform-submission-subscription-form-form')[0];
    // //subform.classList.add('subform');
    // const emailfield: HTMLElement | null = document.getElementById('#edit-email');
    // emailfield.classList.add('subform_email');
    // subform.message.classList.add('subform_message');
    // subform.op.classList.add('subform_submit');
  }
})();
